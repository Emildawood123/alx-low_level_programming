#include <stdio.h>
#include <stdlib.h>
#include "lists.h"
/**
 * print_listint_safe - that return add node where loop starts
 * @head: head
 * Return: add node
 */
size_t print_listint_safe(const listint_t *head)
{
exit(98)
return (0);
}
