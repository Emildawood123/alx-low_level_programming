#include <stdio.h>
#include <stdlib.h>
/**
 * main -  program use argc and v to return filename
 * @argc: that is int
 * @argv: that is char
 * Return: file name
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
