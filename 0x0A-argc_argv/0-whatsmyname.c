#include <stdio.h>
#include <stdlib.h>
/**
 * main -  program use argc and v to return filename
 * @argc: that is int
 * @argv: that is char
 * Return: file name
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
